version: "3.9"

services:
  redis:
    image: redis
    ports:
      - "6379:6379"
  backend:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ../.env
    volumes:
      - ../backend:/app

    depends_on:
      - redis
